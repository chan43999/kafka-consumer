spring:
  application:
    name: kafka-consumer
  main:
    allow-bean-definition-overriding: true
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      groupId: 12345
      auto-offset-reset: latest
    producer:
      bootstrap-servers: localhost:9092
      acks: 1
app:
  delay: 5

server:
  port: 8080
---
spring:
  cloud:
    kubernetes:
      enabled: false
  config:
    activate:
      on-profile: local
  kafka:
    consumer:
      topics: local
    producer:
      bootstrap-servers: prod-3-brokers-kafka-bootstrap-event-stream.itzroks-666000ldq2-6xuhwd-4b4a324f027aea19c5cbc0c3275c4656-0000.hkg02.containers.appdomain.cloud:443
      topic: perftest
      acks: 1
      properties:
        security.protocol: SASL_SSL
        sasl.mechanism: SCRAM-SHA-512
        ssl.protocol: TLSv1.2
        sasl.jaas.config: org.apache.kafka.common.security.scram.ScramLoginModule required username="springboot-kafka" password="L8gGvoe7aotS";
        ssl.truststore.location: /Users/chansuttichujit/.kafka/truststore.jks
        ssl.truststore.password: changeme
        ssl.enabled.protocols: TLSv1.2
        ssl.endpoint.identification.algorithm: HTTPS
        linger.ms: 10
        batch.size: 100000
        buffer.memory: 93554432
management:
  endpoint:
    health:
      show-details: ALWAYS
    restart:
      enabled: true
  endpoints:
    web:
      exposure:
        include: 'health,info,prometheus,metrics,env'
